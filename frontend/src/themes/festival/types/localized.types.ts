/**
 * LocalizedText — pour tout contenu éditorial multilingue du thème festival.
 * Les noms propres et titres de morceaux ne passent jamais par ce type.
 */
export interface LocalizedText {
    fr: string;
    en: string;
}
